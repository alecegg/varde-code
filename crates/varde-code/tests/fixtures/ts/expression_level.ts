const port = 8080;
const host = "localhost";

const total = add(port, 10);

const enabled = true;
const missing = null;
const message = `Port ${port}`;

const resolved = config.resolve(host);
const length = resolved.length;
