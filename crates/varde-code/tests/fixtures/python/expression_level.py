# Expression-level fixtures: Call, MemberAccess, Literal, Variable.
import math

port = 8080
host = "localhost"
enabled = True
missing = None
ratio = 3.5

total = math.floor(port) + compute(port, 10)
length = len(host)
