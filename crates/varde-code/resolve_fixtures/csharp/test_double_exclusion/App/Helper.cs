namespace App;

public static class Helper
{
    // The sole production definition of `Compute` — the repo-wide fallback
    // resolves Service.Handle's `Compute()` call here.
    public static void Compute()
    {
    }
}
